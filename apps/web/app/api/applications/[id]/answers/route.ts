import { applicationAnswersPatchSchema } from '@aaa/contracts';
import { requireSession } from '../../../../../lib/auth.ts';
import { appDb, isoNow } from '../../../../../lib/server.ts';
import { apiError, apiOk, bodyHash, findIdempotency, idempotencyKey, idempotencyScope, requestId, requireMutationOrigin, saveIdempotency } from '../../../../../lib/api.ts';
export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) { const rid = requestId(request); if (!(await requireSession())) return apiError('UNAUTHENTICATED', 401, rid); if (!requireMutationOrigin(request)) return apiError('FORBIDDEN_ORIGIN', 403, rid); const expected = request.headers.get('if-match'); if (!expected) return apiError('PRECONDITION_REQUIRED', 428, rid); const key = idempotencyKey(request); if (!key) return apiError('IDEMPOTENCY_KEY_REQUIRED', 428, rid); const raw = await request.json().catch(() => null); const parsed = applicationAnswersPatchSchema.safeParse(raw); if (!parsed.success) return apiError('VALIDATION_FAILED', 400, rid); const { id } = await context.params; const db = appDb(); try { const scope = idempotencyScope(request, `/api/v1/applications/${id}/answers`, 'administrator'); const prior = findIdempotency(db, scope, key, bodyHash(raw)); if (prior === 'CONFLICT') return apiError('IDEMPOTENCY_CONFLICT', 409, rid, { entityId: id }); if (prior) return apiOk(prior.body, prior.status, rid); const row = db.prepare('SELECT revision,state FROM application_attempts WHERE id=?').get(id) as { revision: number; state: string } | undefined; if (!row) return apiError('NOT_FOUND', 404, rid, { entityId: id }); const normalized = expected.replace(/^W\//, '').replaceAll('"', ''); if (normalized !== String(row.revision) && expected !== '*') return apiError('REVISION_CONFLICT', 409, rid, { entityId: id }); if (!['queued', 'preparing', 'needs_review', 'needs_input'].includes(row.state)) return apiError('STATE_CONFLICT', 409, rid, { entityId: id }); const revision = row.revision + 1; const now = isoNow(); const data = { id, revision, answers: parsed.data.answers, state: row.state, updatedAt: now }; db.transaction(() => { db.prepare('UPDATE application_attempts SET revision=?,answers_json=?,updated_at=? WHERE id=?').run(revision, JSON.stringify(parsed.data.answers), now, id); saveIdempotency(db, scope, key, bodyHash(raw), 200, data, now); })(); return apiOk(data, 200, rid); } finally { db.close(); } }
